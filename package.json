{
  "name": "node-ytdlp",
  "version": "1.0.0",
  "scripts": {
    "build": "ts-node --transpile-only build && tsc -D index.ts",
    "test": "ts-node --transpile-only test.ts"
  },
  "devDependencies": {
    "@types/node": "^17.0.17",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  }
}